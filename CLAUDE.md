# Italia in Tavola — handover for any assistant continuing this project

Read this whole file before touching anything. It is written so a fresh session (any model) can continue
without the original conversation. `README.md` is the user-facing summary, `PLAN.md` the roadmap; this file is
the operating manual.

## 1. What this is

A mobile-first course on Italian regions, wine and food. The navigation is a 3D relief map of Italy
(Three.js r128, real elevation data). Tapping a region opens a module sheet; each module has four
illustrated, narrated readings in English and Norwegian (bokmål). Everything is one HTML file plus content
and asset files; a build step produces a single-file version for the hosted preview.

- Owner: Vidar (Norwegian). Both languages matter equally. Mobile first, always test on a phone viewport.
- Done: Lazio (IT-62), Piemonte (IT-21), Toscana (IT-52), Veneto (IT-34), Campania (IT-72), Sicilia (IT-82), Lombardia (IT-25), Emilia-Romagna (IT-45), Puglia (IT-75): text EN+NO, photos, narration EN+NO.
- Next: the remaining eleven in batches (suggested order: Trentino-Alto Adige, Friuli, Liguria, Umbria, Marche, Abruzzo, Sardegna, Calabria, Basilicata, Molise, Valle d'Aosta).
- Live site (the real course, full narration, no size limit): https://vidarsveen.github.io/italia-in-tavola/
  Source repo: https://github.com/vidarsveen/italia-in-tavola (public, branch `main`). Every push to `main` runs
  `.github/workflows/pages.yml`, which runs `tools/make_site.py` and publishes `site/` to GitHub Pages (2–3 min).
  So the deploy step for a new region is simply: commit and `git push`. `dist/`, `site/` and the 33 MB DEM are
  git-ignored.
- Hosted preview (Claude artifact, 16 MB cap, browser voice for most regions): https://claude.ai/code/artifact/f01358a9-a2b1-40b6-8232-aa3d55343263
  Republish with the Artifact tool passing this `url`, file `dist/italia-course.html`. Never publish without
  `url` (that creates a second artifact).
- A separate older prototype, the cartoon planet game `index.html`, is unrelated; leave it alone.

## 2. Files

| Path | Role |
| --- | --- |
| `italia-course.html` | the app: CSS, HTML shell, and one big script (see §3). Loads content and assets as separate files at runtime. |
| `content/<region>.js` | English readings: `window.READINGS['IT-xx'] = {credits, lessons:[4]}`. |
| `content/<region>.no.js` | Norwegian readings: `window.READINGS_NO['IT-xx']`; reuses the English credits object. |
| `content/course.no.js` | Norwegian summaries for all 20 regions (intro, landmark, pairing, lesson titles). English summaries live in the app's `COURSE` object. |
| `assets/<region>/*.jpg` + `credits.json` | photos, 820 px wide, JPEG q60, Wikimedia Commons CC/PD only. |
| `assets/audio/<region>/` | `<lang>-<n>.mp3` (48 kbit, self-hosting), `<lang>-<n>.ogg` (Opus 12 kbit, hosted page), `<lang>-<n>.txt` (spoken script), `manifest.json`, `manifest.js` (generated by build). |
| `assets/terrain/` | `height.png` (16-bit heightmap packed in R,G, value = m + 6000), `relief.jpg` (baked shaded relief), `meta.json` (crop box in zoom-8 Web Mercator pixels). Baked by `tools/bake_terrain.py` from `docs/dem_italy_z8.npy`. |
| `docs/regions.min.json` | simplified region polygons (lon/lat) from openpolis ISTAT data; also embedded in the app as `REGIONS`. |
| `docs/italia-course.v2-extruded.html` | the previous flat-extrusion map, kept for reference only. |
| `build.py` | makes `dist/italia-course.html`: inlines content scripts, audio manifests, photos, terrain and the Opus narration of the regions listed in `HOSTED_AUDIO`. |
| `tools/make_site.py` | makes `site/` (plain files, full-quality MP3, no size limit) for self-hosting. |
| `tools/narrate.py`, `tools/opus.py` | narration pipeline (§6). |
| `tools/test/` | headless-Chrome screenshot and touch tests (§8). |
| `three.min.js` | local fallback copy of Three.js r128 (the page loads it from cdnjs first). |

## 3. Anatomy of italia-course.html

Top to bottom: `<style>` (mobile-first, desktop rules under `@media (min-width:900px)`), HTML shell
(`#top`, `#labels`, `#rail`, `#sheet`, `#homeBtn`, `#tiltBtn`, `#tip`, `#reader` with `.rplayer`),
`<script>` with `const REGIONS = [...]` (polygons), `<!--__IMAGES__-->` (build injects `IMAGE_DATA` /
`AUDIO_DATA` maps), the content `<script src>` tags, then the main IIFE. Section markers inside the IIFE,
in order: LANGUAGE (`I18N`, `lang`, `T()`, `course()`, `readingsFor()`, `applyLang()`), COURSE SUMMARIES
(`COURSE`, `ORDER`, `ASSET_DIRS`), PROJECTION (Web Mercator `merc()`, `X(lon)`, `Z(lat)`, `elevAt`, `yAt`,
`LABEL_POS`, `FAMILY` colours), RENDERER, TERRAIN (`buildTerrain()`: heightmap mesh, overlay canvas with cased
borders, region-ID canvas, ShaderMaterial with `sel/hov/dim` uniforms), PRIMITIVES + LANDMARK BUILDERS (`B.*`,
one per region, primitives only), LANDMARKS (pins at overview, models when focused, HTML labels), CAMERA
(`HOME()`, `flyTo`, `frameRegion`, pointer/pinch handlers), PICKING (`groundHit` + `regionAt` via the ID
raster, tolerant snapping), PROGRESS (localStorage), REGION LIST, REGION SHEET (`select`, `closeRegion`,
`home`, `step`), READER (`openReader`, `figureHtml`, `narrationText`), AUDIO SOURCES (`OGG_OK`,
`oggOpusToCaf`, `resolveAudio`), AUDIO PLAYER, ROUTER (`#/IT-62`, `#/IT-62/3`), LOOP + `begin()`.

Debug URL parameters: `?instant` (skip font wait and camera tweens), `region=IT-62`, `lesson=3`, `lang=no`,
`scroll=2000`, and `window.__dbg` exposes `pick`, `groundHit`, `regionAt`, `oggOpusToCaf`, `resolveAudio`.

## 4. Adding a region (the exact recipe used for Piemonte and Toscana)

1. **Text, English.** Create `content/<region>.js` by copying `content/toscana.js`. Keep the structure:
   `credits: /*__CREDITS__*/{}` placeholder, four lessons, each with `title, kicker ("Wine · Reading 1 of 4"),
   minutes, hero, heroCaption, summary, html`. The `html` is a template literal with: `<p class="lead">`,
   `<h2>` sections, `<figure data-img="key"><figcaption>…</figcaption></figure>` (no `<img>`; the app resolves
   `key` to `assets/<region>/key.jpg`), `<aside class="facts"><h4>Key facts</h4><ul>…`, one
   `<aside class="tasting"><h4>In the glass: …</h4><table><tr><th>Colour</th><td>…` per wine lesson, and a
   closing `<div class="recap"><h4>Before you move on</h4><ul>` with three bullets. Lesson themes follow the four
   titles already listed for the region in `COURSE` (wine, wine, food, landmark/food). 600–800 words each,
   factual, specific, no marketing tone; check dates, DOC/DOCG rules and ageing requirements carefully.
   Never use the class name `glass` inside reading HTML (it collides with the UI panel class).
2. **Text, Norwegian.** `content/<region>.no.js`: `window.READINGS_NO['IT-xx'] = {credits: window.READINGS['IT-xx'].credits, lessons:[…]}`.
   Bokmål, same structure and image keys, kickers "Vin · Lesetekst 1 av 4" / "Mat ·" / "Landemerke ·",
   headings "Nøkkelfakta", "I glasset: …", "Før du går videre", table rows Farge/Duft/Smak/Alkohol/Serveres/Ved bordet.
   Keep Italian names of wines, dishes and places untranslated.
   Write the Norwegian from the facts, not from the English sentence; see §4b below, and re-read it before
   writing any Norwegian. When the file is written, run the review skill on it: `/norsk-review <region>`
   (instructions in `.claude/skills/norsk-review/SKILL.md`, helper `tools/review_no.py`: `--report` dumps EN and NO
   side by side with lint hits, `--apply patch.json` applies exact replacements and re-checks structure, `--stale`
   lists narrations whose spoken script no longer matches the text, `--narrate` regenerates them with a
   truncation retry). After ANY edit to a reading, `python tools/review_no.py <region> --stale` tells you which
   audio files are stale; never guess. All six Norwegian files were language-reviewed in September 2026 and every
   reading needed fixes, so the rules there are not optional.
3. **Photos.** Commons free-text search often returns nothing for Italian phrases (it failed for Etna vineyards,
   arancini, Marsala and alberello). When it does, list a Commons category instead:
   `action=query&list=categorymembers&cmtitle=Category:Arancini&cmtype=file&cmlimit=30`, then fetch imageinfo for
   those titles. Otherwise search Wikimedia Commons through its API with a User-Agent header (it rate-limits otherwise;
   sleep 2 s between calls):
   `https://commons.wikimedia.org/w/api.php?action=query&list=search&srnamespace=6&srlimit=6&format=json&srsearch=<term> filetype:bitmap`
   then `action=query&prop=imageinfo&iiprop=url|extmetadata|size&iiurlwidth=1000&titles=File:…|File:…` to get
   `thumburl`, `Artist`, `LicenseShortName`. Accept only CC0, CC BY, CC BY-SA, public domain; skip photos whose
   author attaches extra conditions. Download the 1000 px thumbnails, LOOK at each one (Read the file) and reject
   weak ones. Save with PIL to `assets/<region>/<key>.jpg` at max 820 px, quality 60, progressive; write
   `credits.json` `{key:{artist, license, source}}` and paste the same object into the `/*__CREDITS__*/` slot.
   About 10–12 photos per region; the hero of each lesson should be strong.
4. **Wire it.** In `italia-course.html`: add `'IT-xx':'<region>'` to `ASSET_DIRS`; add three script tags after
   the previous region's: `content/<region>.js`, `content/<region>.no.js`, `assets/audio/<region>/manifest.js`.
5. **Narration.** `python tools/review_no.py <region> --narrate` regenerates every stale file with a truncation
   retry and re-encodes Opus (preferred). Underneath it is `python tools/narrate.py <region>` (≈2 min per lesson, run in the background; needs
   `edge-tts` and `imageio-ffmpeg`, install with `python -m pip install`). Voices: en-GB-SoniaNeural,
   nb-NO-PernilleNeural. Then `python tools/opus.py <region> 12`. Regenerate one file with `--only no-3`.
6. **Build and test.** `python build.py` (writes dist/ and the audio `manifest.js` files), then §8 tests,
   then `python tools/make_site.py`.
7. **Hosted size.** `dist/italia-course.html` must stay under 16 MB; it is at 11.8 MB after Puglia (nine regions
   of photos, `HOSTED_AUDIO = []`, so no narration is inlined and every region uses the browser voice there). The arithmetic that matters: one region's Opus narration costs about
   4.5 MB inlined, its photos only about 0.8 MB, so narration is what to cut. `HOSTED_AUDIO` in build.py was
   ['lazio','piemonte'] and became ['lazio'] when Campania was added. Encode photos at 720 px, JPEG quality 38–62,
   about 40 KB each. build.py inlines every .jpg in every `assets/<region>/` folder, referenced or not, so keep
   stray files out. The page offers "Listen with the browser voice" for regions without inlined narration, which
   is the intended behaviour. Full narration for every region is on the live site.
8. **Publish.** Artifact tool, `file_path` = dist file, `url` = the artifact URL above, a short `label`.
9. **Docs.** Update README (regions done), PLAN §6c status, and the memory note if something non-obvious was learned.

### 4b. Norwegian style rules (learned from the September 2026 language review)

Every one of the first six regions was drafted by carrying English sentence structure into bokmål, and every
reading had to be repaired and its narration re-recorded. Judge each sentence on whether a Norwegian writer
would have produced it, not on whether it is a faithful rendering of the English. Read it aloud without the
English in view; if it needs the English to parse, rewrite it.

1. **No clefts.** "What makes it work is altitude" / "The story of the last forty years is how…" become plain
   sentences with the real subject first: «Når det lykkes, gir den …», «De siste førti årene handler om …».
   Never «Det som …, er …» as a default opener.
2. **Adjectives and comparatives need a noun.** "is warmer and earlier", "gives darker, longer-lived",
   "red-fruited and moderate in alcohol" are not Norwegian sentences. Write «druene modner tidligere»,
   «gir mørkere viner som lever lenger», «med rød frukt og moderat alkohol». Check that every «en … en»
   and every bare comparative has something to attach to.
3. **No colon-led apposition.** "The pioneers have names: X, Y" and "Cannubi, Brunate, Ginestra: for a
   Piedmontese…" become full sentences: «Navn som Cannubi, Brunate og Ginestra er …». A colon only introduces
   a list or a direct explanation.
4. **No bare-infinitive subjects.** «Å arbeide her er en kalkulert risiko» → «Den som arbeider her, tar en
   kalkulert risiko». Recast with «Den som …, må …» or a noun subject.
5. **English participle phrases become relative clauses.** "racks called arele", "skins left in the vat",
   "eaten in salads, made into…", "Vesuvius, still active, …" → «som kalles», «som blir liggende», «som
   spises i salater og brukes til», «som fortsatt er aktiv». Never open a sentence with a participle chain
   («Ravgul, nøtteaktig og søt, skjenkes den …»).
6. **Imperative-plus-"and" openers become conditionals.** "Drive north from Verona and the plain breaks up"
   → «Kjører du nordover fra Verona, brytes sletten opp».
7. **Word order.** Verb second; «aldri», «ikke», «fortsatt» after the subject in main clauses («kunne vinlusen
   aldri», not «kunne aldri vinlusen»). Sentence adverbs like "more importantly" do not sit between commas:
   write «og viktigst av alt:» or start a new sentence. Comma after a fronted subordinate clause or a long
   relative-clause subject («Det de bar med seg, måtte …»).
8. **Wine and food vocabulary.** «duften», not «nesen»; «blomsterpreget», not «blomstrende»; «på fat» /
   «på trefat», never «på tre»; «drikkeklar», «tåler lagring», «mer røykpreget» (not «røykere»); «pålegg»
   on a pizza, not «fyll»; «oksekjøtt», not «okse»; «garantert tradisjonell spesialitet» for TSG;
   «lagret den» for "laid it down"; «sterkvin», not «forsterket vin»; «opphøyd til DOCG», not «forfremmet»;
   «De eoliske øyer», not «Liparøyene». «Pasta» means pasta, so "a paste of cheese" is «en jevn masse».
9. **Stock calques to avoid.** "the villages to know" → «landsbyene man bør kjenne»; "as celebrated as
   anything" → «like berømte som noen»; "the locals" → «folk fra traktene»; "takes a white" → «vil ha en
   hvitvin»; "within a year" → «i løpet av et år»; "the door to" → «porten til»; "expect cherries" → «regn
   med kirsebær»; "an embarrassment" → «pinlig»; "a wink to" → «et nikk til»; "blamed on" → «forklart med»;
   "not so much X as Y" and "one of the easiest wines to buy badly" have no Norwegian form, rewrite them;
   "smaker likt" means tastes alike, "smaker det samme" means tastes the same; "et halvt kokt egg" reads as
   half-cooked, write «et halvt hardkokt egg».
10. **Numbers and names.** Centuries as «1900-tallet», never «det tjuende århundret»; vintages as
    «1888-årgangen». Compounds are one word («enkelttradisjon», «alkoholstyrke», «sfogliaen», not
    «sfoglia-en»). Watch for nynorsk plurals slipping in («buegangar» → «bueganger»). «buegang» is a count noun
    (62 km med bueganger); Italian loanwords like batteria, acetaia, osteria, trattoria take «en». More calques:
    "winter brings" → «om vinteren kommer», "X matters" → «betyr mye», "owes X to" → «takket være», "and a good
    one" → repeat the noun, "as little as" → «bare», "said to be" → «angivelig», "a wine of the year" → «en vin
    som skal drikkes samme år».
11. **«hvis» is not a relative pronoun** in running prose («en villa hvis mosaikker …»). Use «der», «med» or
    «som … til». Also avoid «pluss», «UNESCO-listet», «nådd via»; write «på verdensarvlisten», «som nås fra».

## 5. Map facts you need if you touch the 3D code

- Projection is Web Mercator matched to the baked terrain crop (lon 6.0–19.2, lat 36.3–47.5, zoom-8 pixels).
  World units: map height 110 units; ~11.4 km per unit; vertical exaggeration 4×. `X(lon)`, `Z(lat)`, `yAt(x,z)`.
- Region tint families: `FAMILY` (North/Centre/South/Islands). Selection and hover are shader uniforms driven by
  the region-ID raster; picking reads the same raster on the CPU with a snap radius of 8 raster pixels.
- Labels are HTML (`#labels .lbl`), positioned every frame; hand-placed anchors in `LABEL_POS`.
- Landmarks: pins (sprites) at the overview; `B.<builder>` model groups only when the region is selected and
  `ctl.dist < 75`. Volcanoes and mountains are real terrain now; builders should not add fake peaks.
- Camera: north-up, `phi` 0.3–0.42 by default, one-finger/left-drag pans, right/alt-drag orbits, `◭` tilts.
  Portrait home: target (9, 0, 3), dist 268. `frameRegion` fits the region above the sheet on phones.
- To rebuild terrain assets: `python tools/bake_terrain.py` (needs numpy, PIL, `docs/dem_italy_z8.npy`;
  the DEM came from AWS Terrain Tiles, Terrarium PNG, zoom 8, tiles x 132–141, y 88–100, no API key).

## 6. Audio facts

- `tools/narrate.py` turns each lesson into a spoken script (figures and credits dropped, tables read as
  "Colour: …", headings as pauses), synthesises MP3 via Microsoft Edge neural voices (unofficial free route,
  fine for prototype; production should use a licensed TTS or a human narrator), writes durations to `manifest.json`.
- `tools/opus.py` makes `.ogg` (libopus, 12 kbit, VOIP tuning, 40 ms frames). The hosted page inlines those.
- Safari before iOS 18.4 cannot play Opus in Ogg. The page repackages the Ogg packets into Apple's CAF container
  in JavaScript (`oggOpusToCaf`), verified byte-identical to ffmpeg's `-f caf` output minus its `info` chunk.
  Do not change that function without re-running `tools/test/caftest.py`.
- Self-hosted (`site/`, or the source tree served as files) plays the MP3s directly.
- The artifact document store (`db` capability) is NOT a solution for audio: 256 KB per document and it makes
  the artifact organisation-internal (link not shareable).

## 7. UI conventions

- Fonts: Fraunces (display) + Manrope (UI/body) from Google Fonts; a single dark "dusk atlas" theme for the map,
  a light page for the reader. Wine-red accent `--wine`, olive for done states, gold for eyebrows.
- Strings live in `I18N` (`en`, `no`); use `T('key')`, never hardcode English in dynamic HTML.
- Sheet states on phones: `peek` (96 px), `half` (56 vh), `full` (90 vh), by height, not by translate.
- The page is served without a doctype (the artifact wrapper adds one). In quirks mode tables reset `color`
  and `font`; the `.tasting table` rule sets them explicitly. Keep that.
- Buttons inside `#top` need `pointer-events:auto` (`#top` itself is pointer-events none).

## 8. Testing (the Chrome extension cannot see this page; use headless Chrome)

Serve the folder: `python -m http.server 8765 --bind 127.0.0.1` (run in the background, stop it afterwards).
Chrome path: `C:\Program Files\Google\Chrome\Application\chrome.exe`. Set `PYTHONIOENCODING=utf-8` when a
script prints arrows or Norwegian letters on Windows.

- Screenshot: `tools/test/shot3.sh <name> 390,844 "?instant&region=IT-52&lesson=2&lang=no"` (phone) or `1200,720`
  (desktop). Output PNG lands next to the script; look at it with the Read tool. `--screenshot` needs an absolute
  Windows path (the script handles it). The capture fires about two seconds after load, so use `?instant`.
- Touch flows over the DevTools protocol (needs `websocket-client`): `python tools/test/touchtest.py "<url>"`
  (chip → lesson → back → expand → lesson), `maptaptest.py` (taps on the terrain), `playertest.py` and
  `playertest_no.py` pattern (Listen → player state → skip → pause), `toggletest.py` (language switch),
  `caftest.py` (CAF repackager hash check; it compares against `tools/test/ref.caf`, regenerate that with
  `ffmpeg -i assets/audio/lazio/en-2.ogg -c:a copy -f caf tools/test/ref.caf` if it is missing). Each uses its own Chrome profile dir; run at most three in parallel.
- Always test: phone portrait overview shows all 20 regions; tapping a region on the terrain selects it; a lesson
  opens from the sheet; Listen plays (time advances); language switch re-renders the open module.

## 9. Gotchas that cost time before

- Bash heredocs with `\n` inside Python strings: the shell turns them into real newlines. Put patch scripts in
  files, or use `chr(10)`.
- Three.js r128 only: no `CapsuleGeometry`, no `Vector3.randomDirection`; `Sprite.raycast` throws unless
  `raycaster.setFromCamera` ran first (the `aim()` helper does that).
- Commons API: add a User-Agent and pause between calls, or it returns "too many requests".
- `curl` in this Git Bash is broken; use PowerShell `Invoke-WebRequest` or Python `urllib`.
- ffmpeg is a Windows exe: give it Windows paths, not `/tmp`.
- Photo captions must be updated in BOTH language files when a photo is swapped.
- Keep `dist/` under 16 MB; check the size line that `build.py` prints.

## 10. Roadmap (short)

Regions in order: Veneto, Campania, Sicilia, then Lombardia, Emilia-Romagna, Puglia, and the rest. After the
reading pass: capital-city labels and label collision avoidance on the map, tasting cards, recap questions,
glossary, accounts/progress sync, and a self-hosted deployment (`site/`) which removes the 16 MB limit and is
where region four onwards gets its narration.
